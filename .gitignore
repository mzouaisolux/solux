# dependencies
node_modules
node_modules */

# next build (+ iCloud conflict copies like ".next 2", ".next 3")
.next
.next */
.next*

# env / secrets
.env.local
.env*.local
.env.e2e
.env*.bak
*.bak

# E2E harness artifacts (sessions, run reports, traces)
e2e/.auth
e2e/.runs

# sales-analytics import data (real customer financials — never commit)
/data/*.csv

# misc
.DS_Store
*.log
